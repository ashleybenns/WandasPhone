.vercel
.env*.local

# Android / Gradle
build/
.gradle/
local.properties
*.iml
.cxx/

# Firebase / Google (contains API keys — get from Firebase Console per developer)
app/google-services.json

# Release signing (see keystore.properties.example)
keystore.properties
*.keystore
*.jks
